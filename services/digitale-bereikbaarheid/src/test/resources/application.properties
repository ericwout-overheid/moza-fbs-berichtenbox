# Random port voor tests zodat parallelle Gradle modules niet conflicteren
quarkus.http.test-port=0

# Test configuration - overrides for Quarkus test mode
quarkus.native.builder-image=quay.io/quarkus/ubi-quarkus-mandrel-builder-image:jdk-21

# Disable Quarkus Dev Services - we manage containers via BereikbaarheidTestResource
quarkus.devservices.enabled=false

# OIDC disabled for tests (build-time property)
quarkus.oidc.enabled=false
