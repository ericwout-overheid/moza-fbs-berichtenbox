{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "filepath=$(echo \"$CLAUDE_TOOL_INPUT\" | grep -oP '\"file_path\"\\s*:\\s*\"\\K[^\"]+'); case \"$filepath\" in *.env|*.env.*|*.pem|*.key|*.crt|*.p12|*.jks|*gradle-wrapper.jar) echo 'BLOCK: Dit is een gevoelig bestand - handmatig bewerken vereist' >&2; exit 2;; esac"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(./gradlew:*)",
      "Bash(git:*)",
      "Bash(gh:*)",
      "Bash(docker compose:*)",
      "WebSearch"
    ]
  }
}
